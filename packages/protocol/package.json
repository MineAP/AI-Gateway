{
  "name": "@ai-gateway/protocol",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "check": "biome check --write src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-gateway/shared": "workspace:*"
  }
}
