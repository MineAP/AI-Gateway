{
  "name": "@ai-gateway/gateway",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "check": "biome check --write src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-gateway/compatibility": "workspace:*",
    "@ai-gateway/config": "workspace:*",
    "@ai-gateway/provider": "workspace:*",
    "@ai-gateway/protocol": "workspace:*",
    "@ai-gateway/shared": "workspace:*"
  }
}
